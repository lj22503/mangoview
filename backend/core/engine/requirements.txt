# Mangofolio 分析引擎 — Python 依赖
# 安装: pip install -r requirements.txt

# API 服务
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# 数据校验
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 数据源配置
PyYAML>=6.0

# HTTP 请求（数据源调用）
httpx>=0.27.0

# ── 开发/测试依赖 ──
# pip install -r requirements-dev.txt 或 pip install -r requirements.txt[dev]
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27.0          # FastAPI TestClient 需要
