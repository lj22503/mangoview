# MangoView 前端环境变量
# 复制为 .env.local 并填入实际值

# 后端 API 地址（部署时必须设置）
# Vercel 部署：在 Vercel Dashboard → Settings → Environment Variables 中设置
# 本地开发：保持默认 http://127.0.0.1:8003
NEXT_PUBLIC_API_URL=http://127.0.0.1:8003
